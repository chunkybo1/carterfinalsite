"use client";

import CRMForm from "@/components/ui/CRMForm";

interface CaseReviewFormProps {
  dark?: boolean;
}

export const CaseReviewForm = ({ dark: _ }: CaseReviewFormProps) => {
  return <CRMForm />;
};
